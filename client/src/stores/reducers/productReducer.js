const initState = {
  newProduct: null,
  errorMessage: "",
};

const productReducers = (state = initState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};
export default productReducers;
